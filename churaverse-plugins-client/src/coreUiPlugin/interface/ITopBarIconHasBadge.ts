import { IBadge } from '../../textChatPlugin/ui/interface/IBadge'

export interface ITopBarIconHasBadge {
  badge: IBadge
}
export interface IHasBadge {
  badge: IBadge
}
export interface IBadgeHolder {
  badge: IBadge
}
